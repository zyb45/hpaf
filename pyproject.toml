[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hpaf"
version = "0.1.0"
description = "HPAF project"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["hpaf*"]
